singleton class Expressions extends Something { 
    
    variables: {
        list: List[Int]
    }
    
    functions: {
        override someFunction: (f: (x: Int) -> Int, g: (x: Int) -> Bool) -> Bool = {
            this.list.map((x: Int) -> 5 * x)
        }
    }
}
