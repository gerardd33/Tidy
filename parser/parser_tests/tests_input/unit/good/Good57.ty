singleton class Expressions extends Something { 
    
    variables: {
        twice: get (x: Int) -> Int = get (x: Int) -> {
            if (x > 3) then {
                5
            } else {
                8
            }
        }
    }
}
