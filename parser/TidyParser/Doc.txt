The Language TidyParser
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of TidyParser==

===Literals===
Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.


Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.





UpperCaseIdent literals are recognized by the regular expression
`````upper ('_' | digit | letter)*`````

LowerCaseIdent literals are recognized by the regular expression
`````lower ('_' | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in TidyParser are the following:
  | ``False`` | ``Pass`` | ``True`` | ``abstract``
  | ``and`` | ``case`` | ``class`` | ``do``
  | ``else`` | ``extends`` | ``for`` | ``get``
  | ``if`` | ``immutable`` | ``in`` | ``local``
  | ``match`` | ``mutable`` | ``not`` | ``or``
  | ``override`` | ``private`` | ``singleton`` | ``then``
  | ``value`` | ``while`` | ``with`` |

The symbols used in TidyParser are the following:
  | , | { | } | values:
  | variables: | functions: | actions: | [
  | ] | : | = | (
  | ) | -> | . | #
  | - | * | / | +
  | ++ | < | <= | >
  | >= | == | != |

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of TidyParser==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[ClassDecl]//
  | //[ClassIdent]// | -> | **eps**
  |  |  **|**  | //ClassIdent//
  |  |  **|**  | //ClassIdent// ``,`` //[ClassIdent]//
  | //ClassIdent// | -> | //UpperCaseIdent//
  | //[ClassDecl]// | -> | //ClassDecl//
  |  |  **|**  | //ClassDecl// //[ClassDecl]//
  | //ClassDecl// | -> | //ClassType// ``class`` //ClassIdent// //Inheritance// //ClassBody//
  |  |  **|**  | ``abstract`` //ClassType// ``class`` //ClassIdent// //Inheritance// //ClassBody//
  | //Inheritance// | -> | **eps**
  |  |  **|**  | ``extends`` //ClassIdent//
  | //ClassBody// | -> | **eps**
  |  |  **|**  | ``{`` //ValuesSection// //VariablesSection// //FunctionsSection// //ActionsSection// ``}``
  | //ClassType// | -> | ``mutable``
  |  |  **|**  | ``immutable``
  |  |  **|**  | ``singleton``
  | //ValuesSection// | -> | **eps**
  |  |  **|**  | ``values:`` //ValSBody//
  | //VariablesSection// | -> | **eps**
  |  |  **|**  | ``variables:`` //VarSBody//
  | //FunctionsSection// | -> | **eps**
  |  |  **|**  | ``functions:`` //FSBody//
  | //ActionsSection// | -> | **eps**
  |  |  **|**  | ``actions:`` //ASBody//
  | //ValSBody// | -> | //[ValueDecl]//
  |  |  **|**  | ``{`` //[ValueDecl]// ``}``
  | //ValueIdent// | -> | //LowerCaseIdent//
  | //ValueType// | -> | //ClassIdent//
  |  |  **|**  | //ClassIdent// ``[`` //[ClassIdent]// ``]``
  |  |  **|**  | ``get`` //MethodType//
  |  |  **|**  | ``do`` //MethodType//
  | //[ValueDecl]// | -> | **eps**
  |  |  **|**  | //ValueDecl//
  |  |  **|**  | //ValueDecl// ``,`` //[ValueDecl]//
  | //ValueDecl// | -> | //ValueDeclProper//
  |  |  **|**  | ``private`` //ValueDeclProper//
  | //ValueDeclProper// | -> | //ValueIdent// ``:`` //ValueType//
  |  |  **|**  | //ValueIdent// ``:`` //ValueType// ``=`` //Expr//
  | //VarSBody// | -> | //[ValueDecl]//
  |  |  **|**  | ``{`` //[ValueDecl]// ``}``
  | //FSBody// | -> | **eps**
  |  |  **|**  | ``{`` //[FunctionDecl]// ``}``
  | //FunctionIdent// | -> | //LowerCaseIdent//
  | //MethodType// | -> | ``(`` //[ValueDecl]// ``)`` ``->`` //ValueType//
  | //[FunctionDecl]// | -> | **eps**
  |  |  **|**  | //FunctionDecl// //[FunctionDecl]//
  | //FunctionDecl// | -> | ``override`` //FunctionIdent// ``:`` //MethodType// ``=`` //FunctionBody//
  |  |  **|**  | //FunctionIdent// ``:`` //MethodType// ``=`` //FunctionBody//
  |  |  **|**  | ``private`` //FunctionIdent// ``:`` //MethodType// ``=`` //FunctionBody//
  | //FunctionBody// | -> | //Expr//
  |  |  **|**  | ``{`` //Expr// ``}`` //WithValues//
  | //WithValues// | -> | **eps**
  |  |  **|**  | ``with`` //ValuesSection//
  | //ASBody// | -> | **eps**
  |  |  **|**  | ``{`` //[ActionDecl]// ``}``
  | //[ActionDecl]// | -> | **eps**
  |  |  **|**  | //ActionDecl// //[ActionDecl]//
  | //ActionDecl// | -> | ``override`` //FunctionIdent// ``:`` //MethodType// ``=`` //ActionBody//
  |  |  **|**  | //FunctionIdent// ``:`` //MethodType// ``=`` //ActionBody//
  |  |  **|**  | ``private`` //FunctionIdent// ``:`` //MethodType// ``=`` //ActionBody//
  | //ActionBody// | -> | //Expr//
  |  |  **|**  | ``{`` //[Expr]// ``}``
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr// //[Expr]//
  | //Expr// | -> | //Expr1//
  |  |  **|**  | //Expr1// //BooleanOperator// //Expr//
  | //Expr1// | -> | //Expr2//
  |  |  **|**  | //Expr1// //RelationalOperator// //Expr2//
  | //Expr2// | -> | //Expr3//
  |  |  **|**  | //Expr2// ``+`` //Expr3//
  |  |  **|**  | //Expr2// ``-`` //Expr3//
  |  |  **|**  | //Expr2// ``++`` //Expr3//
  | //Expr3// | -> | //Expr4//
  |  |  **|**  | //Expr3// ``*`` //Expr4//
  |  |  **|**  | //Expr3// ``/`` //Expr4//
  | //Expr4// | -> | //Expr5//
  |  |  **|**  | ``not`` //Expr5//
  |  |  **|**  | ``-`` //Expr5//
  | //Expr5// | -> | ``(`` //Expr// ``)``
  |  |  **|**  | //Literal//
  |  |  **|**  | //ValueIdent//
  |  |  **|**  | //LocalValueDecl//
  |  |  **|**  | //LambdaFunction//
  |  |  **|**  | //LambdaAction//
  |  |  **|**  | ``local`` ``.`` //FunctionCall//
  |  |  **|**  | ``local`` ``#`` //FunctionCall//
  |  |  **|**  | //ConstructorCall//
  |  |  **|**  | //GetExpr//
  |  |  **|**  | //DoExpr//
  |  |  **|**  | //ImperativeControlFlow//
  |  |  **|**  | //FunctionalControlFlow//
  | //Literal// | -> | //Integer//
  |  |  **|**  | //Boolean//
  |  |  **|**  | //Char//
  |  |  **|**  | //String//
  |  |  **|**  | //Void//
  | //Boolean// | -> | ``True``
  |  |  **|**  | ``False``
  | //Void// | -> | ``Pass``
  | //LocalValueDecl// | -> | ``value`` //ValueDecl//
  | //LambdaFunction// | -> | ``get`` ``(`` //[ValueDecl]// ``)`` ``->`` //Expr//
  |  |  **|**  | ``get`` ``(`` //[ValueDecl]// ``)`` ``->`` ``{`` //Expr// ``}``
  | //LambdaAction// | -> | ``do`` ``(`` //[ValueDecl]// ``)`` ``->`` //Expr//
  |  |  **|**  | ``do`` ``(`` //[ValueDecl]// ``)`` ``->`` ``{`` //Expr// ``}``
  | //FunctionCall// | -> | //FunctionIdent//
  |  |  **|**  | //FunctionIdent// ``(`` //[FunctionArgument]// ``)``
  | //[FunctionArgument]// | -> | **eps**
  |  |  **|**  | //FunctionArgument//
  |  |  **|**  | //FunctionArgument// ``,`` //[FunctionArgument]//
  | //FunctionArgument// | -> | //Expr//
  | //ConstructorCall// | -> | //ClassIdent//
  |  |  **|**  | //ClassIdent// ``(`` //[FunctionArgument]// ``)``
  | //GetExpr// | -> | //ValueIdent// ``.`` //FunctionCall//
  |  |  **|**  | //ClassIdent// ``.`` //FunctionCall//
  |  |  **|**  | //GetExpr// ``.`` //FunctionCall//
  | //DoExpr// | -> | //ValueIdent// ``#`` //FunctionCall//
  |  |  **|**  | //ClassIdent// ``#`` //FunctionCall//
  |  |  **|**  | //GetExpr// ``#`` //FunctionCall//
  | //ImperativeControlFlow// | -> | ``while`` ``(`` //Expr// ``)`` ``{`` //[Expr]// ``}``
  |  |  **|**  | ``for`` ``(`` //ValueDecl// ``in`` //Expr// ``)`` ``{`` //[Expr]// ``}``
  |  |  **|**  | ``if`` ``(`` //Expr// ``)`` ``{`` //[Expr]// ``}`` //OptionalElseBranch//
  | //OptionalElseBranch// | -> | ``else`` ``{`` //[Expr]// ``}``
  |  |  **|**  | **eps**
  | //FunctionalControlFlow// | -> | ``if`` ``(`` //Expr// ``)`` //ThenBranch// //ElseBranch//
  |  |  **|**  | ``match`` //Expr// ``{`` //[MatchCase]// ``}``
  | //ThenBranch// | -> | ``then`` //Expr//
  |  |  **|**  | ``then`` ``{`` //Expr// ``}``
  | //ElseBranch// | -> | ``else`` //Expr//
  |  |  **|**  | ``else`` ``{`` //Expr// ``}``
  | //[MatchCase]// | -> | **eps**
  |  |  **|**  | //MatchCase// //[MatchCase]//
  | //MatchCase// | -> | ``case`` //Pattern// ``->`` //Expr//
  | //Pattern// | -> | //ClassIdent//
  | //RelationalOperator// | -> | ``<``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>``
  |  |  **|**  | ``>=``
  |  |  **|**  | ``==``
  |  |  **|**  | ``!=``
  | //BooleanOperator// | -> | ``and``
  |  |  **|**  | ``or``


